pipeline:
  defaults:
    candidates: []
    mandatory: false
    terminal: false
    initial: false

  steps:
    step1:
      candidates: [a, b]
      initial: true
      mandatory: true

    step2:
      candidates: [c, d]

    step3:
      candidates: [e, f, g, h]

    step4:
      candidates: [i, j, k]

    step5:
      candidates: [l, m]
      terminal: true
      mandatory: true

partial_ordering:
  - before: step1
    after: step2

  - before: step2
    after: step3

  - before: step1
    after: step4

  - before: step4
    after: step5

constraints:
  - if:
      step1: {}
    require: [step2, step4]
    forbid: [step3]

  - if:
      step1: b
    require:
      - step4: j
    forbid:
      - step5: l

  - if:
      step4: j
    forbid: [step3]
